---
description: Compare and rank multiple offers
---

# Run career-ops ofertas

Compare and rank multiple job offers using the 10-dimension weighted scoring matrix.

Paste the jobs (text or URLs) to compare:

$ARGUMENTS

Load the career-ops skill in `ofertas` mode:

```javascript
skill({ name: "career-ops" })
```
