---
description: Evaluate a single offer (blocks A-F, no auto PDF)
---

# Run career-ops oferta

Run the full A-G evaluation (A-F scoring + G posting legitimacy) on a single job. No automatic PDF generation.

Paste the job text or URL:

$ARGUMENTS

Load the career-ops skill in `oferta` mode:

```javascript
skill({ name: "career-ops" })
```
