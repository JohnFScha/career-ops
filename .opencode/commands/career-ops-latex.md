---
description: Export CV as a LaTeX/Overleaf .tex file
---

# Run career-ops latex

Export a tailored, ATS-optimized CV as a `.tex` file and compile it to PDF via `tectonic` or `pdflatex`.

Pass any tailoring instructions through:

$ARGUMENTS

Load the career-ops skill in `latex` mode:

```javascript
skill({ name: "career-ops" })
```
