---
description: Scan job portals and discover new offers
---

# Run career-ops scan

Scan the configured job portals, filter by title relevance, and add new offers to the pipeline for later evaluation.

Pass any extra arguments through to the skill:

$ARGUMENTS

Load the career-ops skill in `scan` mode:

```javascript
skill({ name: "career-ops" })
```
