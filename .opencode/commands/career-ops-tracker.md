---
description: Application status overview
---

# Run career-ops tracker

Read and display `data/applications.md` -- the applications tracker.

Pass any filter or query:

$ARGUMENTS

Load the career-ops skill in `tracker` mode:

```javascript
skill({ name: "career-ops" })
```
