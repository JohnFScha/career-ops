---
description: Interactive profile/CV onboarding interview
---

# Run career-ops interview

Run the interactive profile & CV onboarding interview. Extracts rich context, project tasks, technologies, and measurable business impact to build or enhance `cv.md`, `config/profile.yml`, and `modes/_profile.md`.

Pass any starting context (optional):

$ARGUMENTS

Load the career-ops skill in `interview` mode:

```javascript
skill({ name: "career-ops" })
```
