---
description: Cover letter -- standalone JD paste or use a report slug
---

# Run career-ops cover

Generate a tailored cover letter. Accepts either a pasted job description or a report slug (`/career-ops cover {slug}` loads the existing evaluation report draft as a starting point).

Arguments:

$ARGUMENTS

Load the career-ops skill in `cover` mode:

```javascript
skill({ name: "career-ops" })
```
