---
description: career-ops command center -- show the menu or run a sub-command
---

# career-ops -- Command Center

Run the career-ops skill in discovery mode (no args shows the menu) or with the requested sub-command:

$ARGUMENTS

Load the career-ops skill:

```javascript
skill({ name: "career-ops" })
```
